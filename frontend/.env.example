# Set to 'true' to use mock data instead of real backend API
# Useful for frontend-only development
VITE_USE_MOCK=false
VITE_FE_ONLY=false

# Backend API URL (only used when VITE_USE_MOCK is false)
VITE_API_URL=http://localhost:8000
