# Mock mode configuration
# This file is used when running: npm run dev:mock or npm run dev:fe-only
VITE_USE_MOCK=true
VITE_FE_ONLY=true

# Backend API URL (not used in mock mode, but kept for reference)
VITE_API_URL=http://localhost:8000
